# 服务指标

> 应用层可观测性——QPS、错误率、P99 延迟、JVM 指标。

---

## 当前能力

| 能力 | 状态 | 说明 |
|------|------|------|
| Java 服务 Micrometer | 🔴 未覆盖 | 二进制 Java 服务未接入 Micrometer |
| Java 服务 JMX | 🔴 未覆盖 | 未部署 JMX Exporter |
| Python 服务指标 | 🔴 未覆盖 | ai-backend 未暴露 /metrics |

## 推荐引入

1. **Java 服务**：引入 Micrometer + Prometheus Registry，暴露 `/actuator/prometheus`
2. **Python 服务**：使用 `prometheus_client` 库暴露 `/metrics`
3. **RED 指标**：每个服务至少暴露 Rate（QPS）、Errors（错误率）、Duration（P99 延迟）
